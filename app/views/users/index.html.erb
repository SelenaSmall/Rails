<p id="notice"><%= notice %></p>

<body>
<h1><center>USERS</center></h1>
<p>This page will show all users assoiated with the organisation. They can be searched, ordered, viewed. List will ideally be linked to FB or other social media with profile pics/links/etc</p>

<!-- Users Search -->
<div class="row">
<div class="col-md-8"></div>
<div class="col-md-3">
<form class="form" role="search">
  <div class="form-group">
    <input type="text" class="form-control" placeholder="Search Users">
  </div>
</div>
  <button type="submit" class="btn btn-default">Search</button>
</form>
<!-- Users Search -->

<table class="table center">
  <thead>
    <tr>
      <th>User Name</th>
      <th>Password</th>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Email</th>
      <th>Phone</th>
      <th>Facebook</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.user_name %></td>
        <td><%= user.password %></td>
        <td><%= user.first_name %></td>
        <td><%= user.last_name %></td>
        <td><%= user.email %></td>
        <td><%= user.phone %></td>
        <td><%= user.facebook %></td>
        <td><%= link_to 'Details', [@venue, @user]%></td>
        <td><%= link_to 'Edit', edit_user_path(@venue, user) %></td>
        <td><%= link_to 'Delete', [@venue, @user], method: :delete, data: { confirm: 'Are you sure?' } %></td>        
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New', new_venue_user_path(@venue) %>

<!-- Modal form / New Channel Group -->
<div class="modal fade" id='venue-details-modal-form' role="dialog"></div>

<div class="modal fade" id='opening-hours-modal-form' role="dialog"></div>
<!-- Modal form / New Channel Group -->

</body>