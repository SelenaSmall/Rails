<%= form_for [@venue, @section] do |f| %>
  <% if @section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
      <% @section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
    <th>
  <th class="field"><%= f.label :section %><br><%= f.text_field :section %></th>
  <th class="field"><%= f.label :first_num %><br><%= f.text_field :first_num %></th>
  <th class="field"><%= f.label :last_num %><br><%= f.text_field :last_num %></th> 
    </th>
    <th class="field"><%= f.label :bookable %><br><%= f.text_field :bookable %></th> 
    </th>
  </table>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
