<%= form_for [@organisation, @venue, @section] do |f| %>
  <% if @section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
      <% @section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
    <th>
  <th class="field"><%= f.label :sections %><br><%= f.text_field :sections %></th>
  <th class="field"><%= f.label :sections_group %><br><%= f.text_field :sections_group %></th>
  <th class="field"><%= f.label :turn_time %><br><%= f.text_field :turn_time %></th> 
    </th>
  </table>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
