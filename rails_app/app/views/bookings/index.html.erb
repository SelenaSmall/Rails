<p id="notice"><%= notice %></p>

<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">MENU <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'Details', [@venue.organisation, @venue] %></li>
            <li><a href="#">Bookings</a></li>
            <li><%= link_to 'Customers', organisation_venue_customers_path(@venue, @organisation) %></li>
            <li><a href="#">Users</a></li>
            <li><a href="#">Reports</a></li>
            <li><%= link_to edit_all_organisation_venue_notes_path(@organisation, @venue, @covers), 
                remote: true, id: "new-item-link", class: "btn btn-sm btn-default round" do %> Notes
                <%end%></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to 'Organisation', organisation_path(@venue) %></li>
          </ul>
        </li>
      </ul>

      <class="btn-group" role="group" aria-label="...">
      <button type="button" class="btn btn-default navbar-btn">Grid View</button>
      <button type="button" class="btn btn-default navbar-btn">Floor Plan</button>
      <button type="button" class="btn btn-default navbar-btn">Date Selector to go Here</button>

      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">USERS <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="#">List </a></li>
              <li><a href="#">of </a></li>
              <li><a href="#">Users</a></li>
              <li role="separator" class="divider"></li>
              <li><a href="#">Create New User</a></li>
            </ul>
      </ul>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<body>
<h1> BOOKINGS</h1>

<p>This Page will show a runsheet view of all bookings for a specified day in order of time. It will be clear which tables have been seated, with options to view history for the night and new groups waiting to be seated.</p>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Phone</th>
      <th>Email</th>
      <th>Start time </th>
      <th>Tab</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bookings.each do |booking| %>
      <tr>
        <td><%= booking.name %></td>
        <td><%= booking.phone %></td>
        <td><%= booking.email %></td>
        <td><%= booking.start_time %></td>
        <td><%= booking.tab_name %></td>
        <td><%= link_to 'Details', [@organisation, @venue, @booking] %></td>
        <td><%= link_to 'Edit', edit_organisation_venue_booking_path(@organisation, @venue, booking) %></td>
        <td><%= link_to 'Delete', [@organisation, @venue, @booking], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Booking', new_organisation_venue_booking_path %>
<div><%= link_to 'Back to Venues', organisation_venues_path %></div>

<!-- Modal form / New Channel Group -->
<div class="modal fade" id='venue-details-modal-form' role="dialog"></div>

<div class="modal fade" id='opening-hours-modal-form' role="dialog"></div>
<!-- Modal form / New Channel Group -->

</body>
