<p id="notice"><%= notice %></p>

<div class="container">
<%= yield %>
</div>

<h1>VENUES</h1>
<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Phone</th>
      <th>Email</th>
      <th>Address</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @venues.each do |venue| %>
      <tr>
        <td><%= venue.name %></td>
        <td><%= venue.phone %></td>
        <td><%= venue.email %></td>
        <td><%= venue.address %></td>
        <td><%= link_to 'Details', [@organisation, venue] %></td>
        <td><%= link_to 'Bookings', organisation_venue_bookings_path(@organisation, venue) %></td>
        <td><%= link_to 'Customers', organisation_venue_customers_path(@organisation, venue) %></td>
        <td><%= link_to 'Opening Hours', organisation_venue_hours_path(@organisation, venue) %></td>
        <td><%= link_to 'Covers', organisation_venue_covers_path(@organisation, venue) %></td>
        <td><%= link_to 'Tables', organisation_venue_tables_path(@organisation, venue) %></td>
        <td><%= link_to 'Delete', [@organisation, venue], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Venue', organisations_venues_path(@organisation, @venue) %> |
<%= link_to 'Back to Organisations', organisations_path %>
